# AI News Assistant Backend Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=development

# API Configuration (your existing setup is already integrated into the code)
# The bearer token and API URL are already configured in ai-client.js
# You can move them here if you prefer environment-based configuration:

# API_URL=https://enjoyed-boss-grouse.ngrok-free.app/v1/chat/completions
# BEARER_TOKEN=your_bearer_token_here

# Optional: Enable detailed logging
DEBUG=true